;;; Directory Local Variables for the Strayforge blog.
;;; ox-hugo will export every Org file in this project into the Hugo
;;; site rooted here, so C-c C-e H H "just works" from any .org buffer.
((org-mode . ((org-hugo-base-dir . "/Users/xiaofeihu/tech-blog/")
              ;; Use TOML front matter to match the rest of the site's config.
              (org-hugo-front-matter-format . "yaml"))))
